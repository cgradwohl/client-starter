import React from 'react';

const DesktopLayout = ({ children }) => {
  console.log('chilren', children);
  return (
    // TODO: do flex layout stuff here for desktop layout
    <div>
      {children}
    </div>
  );
};

export default DesktopLayout;
